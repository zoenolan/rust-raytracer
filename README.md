# Ray tracing starter template (Rust)

This is the ray tracing starter template for Rust.

## Building and running

### Debug mode

- Build: `cargo build`
- Run: `cargo run`

## Release mode

- Build: `cargo build --release`
- Run: `cargo run --release`
